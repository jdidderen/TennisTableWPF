<controls:MetroWindow x:Class="TennisTableWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local1="clr-namespace:TennisTableWPF.Views"
        xmlns:VM="clr-namespace:TennisTableWPF.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="768" Width="1024">
    <controls:MetroWindow.Background>
        <ImageBrush ImageSource="Resources/background.jpg"/>
    </controls:MetroWindow.Background>
    <DockPanel>
        <Grid DataContext="{Binding TabViewModel, Source={StaticResource Locator}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Expander Padding="0"
                      Margin="4"
                      BorderBrush="DarkGray"
                      ExpandDirection="Right"
                      Grid.Row="0"
                      Grid.Column="0"
                      IsExpanded="True" Background="#CC119EDA">
                <Expander.Header>
                    <TextBlock Text="Menu"
                               FontSize="14"
                               FontWeight="Bold">
                        <TextBlock.LayoutTransform>
                            <RotateTransform Angle="90" />
                        </TextBlock.LayoutTransform>
                    </TextBlock>
                </Expander.Header>
                <Expander.Content>
                    <Grid HorizontalAlignment="Stretch" Background="#D8808080" Width="168" >
                        <Button x:Name="ShowJoueursButton" Command="{Binding AddToTabJoueursViewCommand}" Content="Afficher les joueurs" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="148" RenderTransformOrigin="0.716,0.5"/>
                    </Grid>
                </Expander.Content>
            </Expander>
            <TabControl Margin="1,4,4,4"
                        ItemsSource="{Binding Path=Tabs}"
                        SelectedItem="{Binding TabsSelected}"
                        Grid.Row="0"
                        BorderBrush="DarkGray"
                        Grid.Column="1"
                        IsSynchronizedWithCurrentItem="True" Background="#D8808080">
                <TabControl.ContentTemplate>
                    <DataTemplate DataType="{x:Type local1:JoueursView}">
                        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="Auto" Height="Auto">
                            <local1:JoueursView />
                        </Grid>
                    </DataTemplate>
                </TabControl.ContentTemplate>
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Header}" VerticalAlignment="Center"/>
                            <Button BorderBrush="{x:Null}" Command="{Binding CloseTabViewCommand}" Foreground="{x:Null}" Height="26" Width="26" VerticalAlignment="Center">
                                <Button.Background>
                                    <ImageBrush ImageSource="pack://siteoforigin:,,,/Resources/CloseTab.png" Stretch="Uniform"/>
                                </Button.Background>
                            </Button>
                        </StackPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>
            </TabControl>
        </Grid>
    </DockPanel>
</controls:MetroWindow>

