<UserControl x:Class="TennisTableWPF.Views.Matchs.MatchsGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TennisTableWPF.Views.Matchs">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" DataContext="{Binding MatchsViewModel, Source={StaticResource Locator}}" MaxHeight="600" MaxWidth="800">
            <ListView ItemsSource="{Binding MatchsView}">
                <ListView.View>
                <GridView>
                    <GridViewColumn Header="Numéro de match" DisplayMemberBinding="{Binding NumMatch}" />
                </GridView>
            </ListView.View>

            </ListView>
    </Grid>
</UserControl>
